describe("Enviar Mandou Bem Use Case Unit Test", ()=> {
  let sut: Usuario

  test("Emite erro se usuario que mandou não existe", () => {

  })

  test("Emite erro se usuario que recebe não existe", () => {

  })

  test("Retorna falso se a mensagem não é valida", () => {

  })

  test("")
})